import React, { Component } from 'react';

class NotMatch extends Component {
  render() {
    return (
        <div className="NotMatch-header">
          <h2>Sorry, URL is illegal...</h2>
        </div>
    );
  }
}

export default NotMatch;
